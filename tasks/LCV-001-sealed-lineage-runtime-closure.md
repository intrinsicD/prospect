# LCV-001 — sealed-lineage/runtime-closure gate

**Status:** complete — canonical `PASS`, independently audited  
**Type:** host-bound, non-scientific research-infrastructure gate

## Objective

Produce a sealed receipt proving that a successor causal assay can consume MM-007's
authenticated frames, causal row identities, and source-current fold normalizers
without executing the fragile historical verifier chain, under the exact audited
lexical Python/NumPy/OpenBLAS host commitments.

## Acceptance

- [x] Canonical `results/LCV-001` stayed absent throughout implementation and private
      validation.
- [x] Preparation descriptor-authenticates and copies the exact 14-file MM-007 tree
      without parent JSON/NPZ parsing or historical import/verification.
- [x] Sibling staging, `RENAME_NOREPLACE`, transactional publication, and descriptor cleanup
      leave no markerless partial destination before rename; a post-rename fsync
      error is returned explicitly as committed-but-durability-unconfirmed.
- [x] The published root and `prepared/` namespace are sealed `0555`; only the
      pre-created `outcomes/` directory permits application-level exclusive creation
      before formal execution; the owning UID/filesystem remain trusted.
- [x] Formal and semantic children preserve the lexical `.venv/bin/python -I -S -B`
      identity inside a transient, bounded cgroup-v2 service.
- [x] Interpreter, prefixes, symlink chain, per-file stdlib/NumPy maps, loaded BLAS,
      environment/thread policy, platform receipt, and exact SVD canary are checked.
- [x] Exact successor-facing parent schemas/cross-links, 477 raw frames, 453 causal
      triples, full R8 digest, and select-before-pool fold normalizers replay using
      LCV-owned code.
- [x] The concrete non-contiguous wrong-layout implementation reproduces its four
      preregistered fingerprints/ULP gaps and is rejected by the real normalizer path.
- [x] Twenty-five mutation/runtime controls cover schema, cross-link, index, frame,
      normalizer, R8, layout, runtime-map, thread, and actual child endpoints.
- [x] The formal child can emit only `PENDING_CGROUP_CLEANUP`; only a matching
      inactive-and-absent cgroup receipt permits parent-side final `PASS`.
- [x] Parent finalization builds, seals, and verifies a private sibling before one
      atomic `RENAME_EXCHANGE`; no final-only file enters canonical before commit.
- [x] Post-exchange receipt-loss reconciles the canonical phase before any write:
      completed or indeterminate state is left untouched for manual inspection, and
      only provably provisional membership may be terminalized.
- [x] Catchable pre-commit failure or invalid cleanup terminal-seals only after an
      exact read-only provisional reconciliation; unexpected namespace state is left
      untouched for manual inspection, and neither path can authorize `PASS`.
- [x] The fault matrix covers every final write, candidate seal/verify, both sides of
      the exchange, preparation publication/fsync, raw supervisor failures,
      interruption, host I/O, final child stdout write/flush, diagnostic-channel
      loss with a dedicated transport exit, strict success/failure channel schemas,
      nested canary spawn/timeout/transport faults, malformed supervisor cleanup,
      malformed supervisor return/exception objects, owned-workspace allocation
      faults, and source drift.
- [x] A completed package has exactly 42 files at `0444`, 17 directories at `0555`,
      an exact self-excluding manifest, and read-only structural/semantic verification.
- [x] Focused LCV suite, Ruff, and mypy pass on the pinned host.
- [x] Independent review approves the final source/config/protocol hashes and writes
      the external pre-real `GO` audit.
- [x] The separately authorized one-shot canonical prepare/run/verify completes.

## Gate result

`PASS` — the separately authorized prepare and formal commands each ran exactly once.
The completed canonical package contains exactly 42 files at `0444` and 17
directories at `0555`; public structural and supervised semantic verification both
reproduce `PASS`, all 25 controls replay, and no provisional, terminal, cgroup, unit,
or process residue remains. Two independent post-result scientist passes accept the
bounded host-specific infrastructure claim.

This licenses only pre-real construction and audit of fresh MM-011 after that
experiment revalidates the host commitments and copies the sealed LCV-001 receipt.
MM-009 remains terminal-invalid. No prediction, fitting, scoring, or scientific
capability claim was tested.

See `docs/research/2026-07-16-lcv001-canonical-pass-audit.md` and the external
execution receipt at
`docs/research/2026-07-16-lcv001-canonical-execution-receipt.json`.

## Claim boundary

`PASS` verifies the replacement infrastructure path and authorizes preparation of a
newly named causal assay. It does not retry MM-009, change a prior scientific
classification, establish portable or self-contained execution, or show that the
causal predictor works.
